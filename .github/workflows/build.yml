name: Build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Use Go 1.13
        uses: actions/setup-go@v1
        with:
          go-version: 1.13.x

      - name: build game
        run: |
          go get github.com/tmedwards/tweego
          export PATH=$PATH:$(go env GOPATH)/bin
<<<<<<< HEAD:workflows/build.yml
          tweego -f sugarcube-2 aeou.html yjb-game -o dist/index.html
=======
          tweego -f sugarcube-2 -o aeou.html yjb-game 

>>>>>>> cb69c68970fed828523f836fcc945d1811acd7eb:.github/workflows/build.yml

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v2
        env:
          PERSONAL_TOKEN: ${{ secrets.GH_PAT }}
          PUBLISH_BRANCH: gh-pages
          PUBLISH_DIR: ./dist

